// This is the user model class.
// Note the variable ! means that this variable is required

export interface IUserModelAngular{
    oauthId: string;
    userId: string;
    goalCreated: number;
    name: string;
    email: string;
  }
  